<p><%= title %></p>

<% if (typeof wedDate == 'object' && wedDate) { %>
  <h2><%= wedDate %></h2>
<% } %>

<!-- schedule -->
<table id="schedule">
  <tr class ="row">
    <th class="col-2">time</th>
    <th class="col-4">event</th>
    <th class="col-4">description</th>
    <th class="col-2"></th>
  </tr>

  <% for (let i = 0; i < data.length; i++) { %>
      <% if(data[i].time !== 'hide') { %>
    <tr class ="row">
    <div class="col-2">
      <td class="col-2"><%= data[i].time %></td>
      <td class="col-4"><%= data[i].item %></td>
      <td class="col-4"><%= data[i].description %></td>
      <td class="col-2">
        <a href="/schedule/<%= data[i].id %>/edit"><button class="edit" type="button" name="button" id="<%= data[i].id %>">edit</button></a>
        <button class="delete" type="button" name="button" id="<%= data[i].id %>">delete</button>
      </td>
    </tr>
    <% } %>
  <% } %>
</table>

<br><br>

<!-- insert form-->
<form action="/schedule" method="post">
  <div>
    <label for="time">Time</label>
    <input type="time" name="time" id="time" required>

    <label for="item">Event</label>
    <input type="text" name="item" id="item" placeholder="event" required>

    <label for="description">Description</label>
    <input type="text" name="description" id="description" placeholder="description">

  <button type="submit">add event</button>
</div>

</form>
